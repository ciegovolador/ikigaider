# vite 8 vs @vitejs/plugin-react's peer range is a known, harmless mismatch;
# our new deps (@wllama/wllama, fake-indexeddb) installed cleanly under it.
# Keep CI's npm ci / install from failing on that peer conflict.
legacy-peer-deps=true
